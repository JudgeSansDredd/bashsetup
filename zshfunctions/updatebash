UPDATE_FILE=~/.bash/update

if test -f "$UPDATE_FILE"; then
    if test `find "$UPDATE_FILE" -mmin +1`; then
        echo "time to update"
    else
        echo "not time to update"
    fi
else
    echo "touching file"
    echo "updated" > "$UPDATE_FILE"
fi